<%-include("partials/header") %>
<!-- content -->
<main class="py-5">
  <div class="container">
    <div class="row">
      <div class="col-md-12">
        <div class="card">
          <div class="card-header card-title">
            <div class="d-flex align-items-center">
              <h2 class="mb-0">404 Error</h2>
              <div class="ml-auto"></div>
            </div>
          </div>
          <div class="card-body">
            <% if(massege){ %>
            <h1><%=massege %></h1>
            <% }else{%>
            <h1>Page Not Found</h1>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</main>
<%-include("partials/footer") %>
